# Test Configuration
server.port = 0 # Assign to random port

db.url = "jdbc:h2:mem:test"
# db.url = "jdbc:postgresql://localhost:5432/postgres"
# db.user = postgres
# db.password = password
# hibernate.physical_naming_strategy=com.github.blquinn.minimalapi.common.jpa.SnakeCaseNamingStrategy

hibernate.show_sql = true
hikari.maximumPoolSize = 10
